"use client";
import {useStore,rupiah} from "@/components/Store";
export default function Reports(){const s=useStore();const closed=s.orders.filter(o=>o.status==="PAID"||o.status==="FULFILLED");const revenue=closed.reduce((a,o)=>a+o.total,0);const profit=closed.reduce((a,o)=>a+(o.total-o.cost),0);const conversion=s.leads.length?Math.round((s.leads.filter(l=>l.status==="CLOSING").length/s.leads.length)*100):0;const byProduct=closed.reduce<Record<string,{qty:number,total:number}>>((a,o)=>{a[o.product]??={qty:0,total:0};a[o.product].qty++;a[o.product].total+=o.total;return a},{});function csv(){const rows=[["Order","Customer","Produk","Total","HPP","Status"],...closed.map(o=>[o.code,o.customer,o.product,String(o.total),String(o.cost),o.status])];const blob=new Blob([rows.map(r=>r.map(x=>`"${String(x).replaceAll('"','""')}"`).join(",")).join("\n")],{type:"text/csv"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="my-control-report.csv";a.click();URL.revokeObjectURL(a.href)}
return <><div className="pageHead"><div><h1>Reports</h1><p>Angka dihitung dari order yang sudah terverifikasi.</p></div><button className="btn primary" onClick={csv}>Export CSV</button></div><div className="grid4"><div className="card"><div className="kpiLabel">Revenue</div><div className="kpiValue">{rupiah(revenue)}</div></div><div className="card"><div className="kpiLabel">Net Profit</div><div className="kpiValue">{rupiah(profit)}</div></div><div className="card"><div className="kpiLabel">Conversion</div><div className="kpiValue">{conversion}%</div></div><div className="card"><div className="kpiLabel">Closing</div><div className="kpiValue">{closed.length}</div></div></div>
<div className="section card tableWrap"><div className="sectionTitle"><h2>Penjualan per produk</h2></div><table className="table"><thead><tr><th>Produk</th><th>Terjual</th><th>Revenue</th></tr></thead><tbody>{Object.entries(byProduct).map(([p,v])=><tr key={p}><td>{p}</td><td>{v.qty}</td><td>{rupiah(v.total)}</td></tr>)}{!Object.keys(byProduct).length&&<tr><td colSpan={3}><div className="empty">Belum ada closing.</div></td></tr>}</tbody></table></div></>}
